interface:
  display_name: "Job Search"
  short_description: "Discover, judge, track, and alert on relevant jobs"
  default_prompt: "Use $job-search to run the configured multi-agent job-discovery workflow."
policy:
  allow_implicit_invocation: true
