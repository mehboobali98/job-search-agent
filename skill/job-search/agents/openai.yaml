interface:
  display_name: "Job Search"
  short_description: "Discover, ingest alerts, assess, and prepare jobs"
  default_prompt: "Use $job-search to discover jobs, preview an opt-in job-alert batch, or inspect a prepared application form."
policy:
  allow_implicit_invocation: true
