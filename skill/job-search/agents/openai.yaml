interface:
  display_name: "Job Search"
  short_description: "Discover, notify, assess, and prepare jobs"
  default_prompt: "Use $job-search to discover jobs, preview a private notification digest or import, or inspect a prepared application form."
policy:
  allow_implicit_invocation: true
